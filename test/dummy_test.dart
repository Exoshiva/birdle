import 'package:flutter_test/flutter_test.dart';

void main() {
  test('Pipeline Dummy Test', () {
    // Dieser Test ist nur da, damit die CI-Pipeline nicht abbricht, 
    // bis die echten JSON- und Logik-Tests geschrieben werden.
    expect(true, true);
  });
}